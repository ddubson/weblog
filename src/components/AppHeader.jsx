import * as React from "react";
import { Navbar } from "react-bootstrap";

const AppHeader = () => (
  <Navbar>
    <Navbar.Header>
      <Navbar.Brand>
        Weblog
      </Navbar.Brand>
    </Navbar.Header>
  </Navbar>
);

export default AppHeader;